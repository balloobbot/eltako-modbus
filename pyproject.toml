[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "eltako-modbus"
version = "0.1.0"
description = "Read an Eltako DSZ15DZMOD three-phase energy meter over Modbus."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "eltako-modbus contributors" }]
keywords = ["modbus", "eltako", "dsz15dzmod", "energy-meter", "home-assistant"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Topic :: Home Automation",
]
dependencies = ["modbus-connection[tmodbus]>=4.8.1"]

[project.urls]
Homepage = "https://github.com/balloobbot/eltako-modbus"

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.24",
    "ruff>=0.15",
    "mypy>=1.14",
]

[tool.hatch.build.targets.wheel]
packages = ["src/eltako_modbus"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "tests", "script"]
